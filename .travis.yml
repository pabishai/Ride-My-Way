language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest 
  - coverage run --source=. app/resources/apis.py test app/tests/test_apis.py
after_success:
  - coveralls

